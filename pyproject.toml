[project]
name = "verbroken-verbinding-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.11.0",
    "streamlit>=1.51.0",
    "transformers>=4.57.1",
]
